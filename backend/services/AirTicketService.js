class AirTicketService {
  static async createMonthlyAccrual(employeeId, year, month, accrual, segment, estimatedCost) {
    // Placeholder: do nothing
    return true;
  }
}
module.exports = AirTicketService;
